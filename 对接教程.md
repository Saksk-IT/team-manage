# Codex API 登录对接教程

> 本 Markdown 教程以当前静态页面 `/codex-guide` 为准同步维护。

## 重要｜站点信息卡

| 项目 | 内容 |
| --- | --- |
| 中转注册页 | <https://api.sakms.top/register> |
| 额度查询页 | <https://api.sakms.top/profile> |
| 卡密自助购买地址 | <https://pay.ldxp.cn/shop/LSSZLMUY> |
| API base_url | `https://api.sakms.top/`；如客户端明确要求 OpenAI-compatible `/v1` 路径，则按“使用密钥”弹窗或客户端提示填写 `https://api.sakms.top/v1`。 |
| API 密钥（api_key） | 自行到中转站“API 密钥”页面创建，格式形如 `sk-xxxx`，请立即保存并妥善保管。 |
| 推荐客户端 | Codex APP / Codex CLI / Codex VSCode 插件；也可接入 Claude Code、Open Code、Open Claw，新手可使用 cc-switch 一键配置。 |

## 📖 阅读指南

本教程把使用流程拆成 **「准备 → 拿 Key → 接入客户端 → 验证」** 四步。无论是通过质保兑换补发的中转兑换码，还是在链动小铺上面购买的额度包兑换码，都适用以下教程。

| 你的身份 | 阅读路径 |
| --- | --- |
| 第一次使用，从零开始 | 第一章 → 第二章 → 第三章对应客户端小节 → 第四章 |
| 已有中转账号，只想配置某个客户端 | 第二章 → 第三章对应客户端小节 |
| 配置后登录失败或限额 | 第四章验证与排错 |

## 提示｜三个名词速懂

- **base_url：** 中转站的前台地址，告诉客户端去哪请求 AI；本教程默认使用 `https://api.sakms.top/`，部分 OpenAI-compatible 客户端需要按弹窗填写 `/v1` 后缀。
- **api_key：** 你的“门票”，形如 `sk-xxxx`，用于区分用户与记录消费；不能泄露，不要发给他人，泄露等于钱包给别人。
- **sub2api：** 把订阅式服务转换为可被代码或客户端调用的 API，本教程中的中转兑换码和订阅权益都可理解为这个接入链路的一部分。

---

# 第一章 准备工作

## 1.1 使用前说明

目前 GPT Team 已全部失效，后续可以接入中转使用 GPT Codex。中转使用纯血 Plus 号池，支持 gpt-5.5，并支持 Image 2.0 生图。

Codex.app 是 GPT 官方推出的强大软件，具备网页版的核心能力，并且在本地客户端中提供更强的使用体验。

**兑换码来源都适用：** 本教程同时适用于按邮箱质保剩余时间补发的中转兑换码，也就是质保补发的中转兑换码，以及在链动小铺购买的额度包兑换码。

### API Key 分组选择规则

| 兑换码类型 | 新建 API Key 时选择 |
| --- | --- |
| 额度兑换码 / 链动小铺购买的额度包 / 余额包兑换码 | 必须选择 `GPT-Plus`，不能选“质保补偿”，否则无法使用。 |
| 通过质保网站补发的中转兑换码 | 选择“质保补偿”分组。 |

### 计费模式提醒

- **订阅模式：** 每天有一定额度，每天 0 点后自动刷新。
- **额度模式：** 使用时消耗网站账户里的额度，用完就没有了。

需要额外购买卡密时，可通过链动小铺自助购买额度包兑换码：<https://pay.ldxp.cn/shop/LSSZLMUY>

有任何问题请扫码加群，联系群主处理：

![问题交流群二维码](image-16.png)

## 1.2 注册中转账户

**操作步骤：** 操作步骤

浏览器打开 https://api.sakms.top/register
填写邮箱、获取验证码、设置密码完成注册

注册链接：<https://api.sakms.top/register>

![中转服务注册页面截图](image.png)

## 1.3 充值与订阅

如果账号内还没有可用权益，可通过站内充值 / 订阅、卡密自助购买，或兑换已发放的中转兑换码获得额度。

- **充值模式：** 按金额充值，按调用量从余额中扣费。
- **订阅模式：** 按天 / 月 / 季 / 年等周期提供固定额度，更适合稳定使用。
- **倍率与套餐：** 不同站点、不同分组可能不同，具体以中转后台实时显示为准。

> 注意：充值、订阅和兑换码都是权益来源；创建 API Key 时仍然要按“质保补偿 / GPT / 订阅-90$”等分组绑定正确权益。

## 1.4 兑换中转码

登录后进入兑换页面，输入提供给你的中转兑换码或额度包兑换码，点击“兑换”。

![兑换成功后的页面截图](image-1.png)

兑换成功后，账户会获得对应余额或权益。

![alt text](image-17.png)

---

# 第二章 创建 API 密钥（所有接入方式通用）

> 重要：本章是所有客户端的前置步骤。无论后面要接 Codex、Claude Code、Open Code 还是 Open Claw，都要先在这里拿到一把自己的 API Key。

## 2.1 创建新密钥并选择正确分组

登录后进入“API 密钥”页面，点击“创建密钥”。名称可按自己需要随便填写，也建议按用途命名，例如 `codex`、`claude-mac`、`opencode-win`，方便后续区分和单独吊销。

### 创建密钥计费模式提醒

| 分组类型 | 说明 |
| --- | --- |
| 订阅类分组：质保补偿 / 订阅** | “质保补偿”和“订阅**”是订阅制度，不需要消耗余额；质保网站补发的中转码选“质保补偿”。 |
| 额度类分组：GPT | 链动小铺购买余额包或额度兑换码时，应选择“GPT-Plus”分组。创建密钥前请再次确认自己使用的是订阅还是额度。 |

> 分组选错会导致无法使用。分组决定密钥使用的权益来源，请先判断兑换码来源，再选择对应分组。

### API 密钥创建步骤

1. 进入“API 密钥”页面，点击“创建密钥”。

   ![alt text](image-18.png)

2. 选择分组：质保网站补发的兑换码选“质保补偿”分组；链动小铺额度兑换码选“GPT”或“订阅**“分组。

   ![alt text](image-19.png)


3. 创建后回到密钥列表，点击“使用密钥”。

  ![alt text](image-20.png)

   进入弹窗后，复制自己的 Codex 配置内容。

## 2.2 一键查看接入配置

密钥创建成功后，在密钥列表的操作列点击“使用密钥”，会弹出接入配置。参考页中会直接给出 Codex CLI、Open Code、Claude Code 等客户端的现成配置；本页也按这些客户端补齐了手动配置方法。

> 重点提示：Codex VSCode 插件、Codex APP 与 Codex CLI 配置完全相同，通常只填一次。弹窗里的配置已经把 `base_url` 和 `api_key` 填好，优先复制弹窗，不要手动拼错。

![使用 API 密钥弹窗中的 Codex 配置示例，密钥已脱敏](image-5.png)

不要复制教程截图中的示例密钥，请使用你自己页面中生成的 API Key。

---

# 第三章 客户端接入（按客户端分）

> 说明：参考页按 Windows / macOS / Linux 三系统分别说明。下面把它有、当前页原本没有的 Claude Code、Open Code、Open Claw 也补齐；你的真实 API Key、base_url 仍以“使用密钥”弹窗为准。

## 3.1 Codex 系列（CLI / VSCode 插件 / APP）

适用范围：**Codex CLI、Codex VSCode 插件、Codex APP**，三者共用同一份 `.codex` 配置。

> 下载好后必须先打开一次 Codex。先打开 Codex 初始化配置文件，然后再完全关闭 Codex，继续下一步文件配置。

下载地址：<https://openai.com/zh-Hans-CN/codex/>

![OpenAI Codex 下载页面截图](image-6.png)

图 7：点击页面中的下载入口获取 Codex。

### 3.1.1 定位 Codex 配置目录

> 配置文件前必须退出登录并完全关闭 Codex。请确保 Codex 进程没有在运行，再编辑 `config.toml` 和 `auth.json`，否则配置可能不会生效。

| 系统 | 配置目录 | 打开方式 |
| --- | --- | --- |
| Windows | `%userprofile%\.codex` | 按 `Win` + `R`，输入 `%userprofile%\.codex` 并回车；文件双击没有反应时，右键选择“记事本”打开。 |
| macOS | `~/.codex` | 终端执行 `open ~/.codex`；如不存在先执行 `mkdir -p ~/.codex`。 |
| Linux | `~/.codex` | 终端执行 `cd ~/.codex` 或直接进入该目录；不存在时先执行 `mkdir -p ~/.codex`。 |

> 提示：如果目录或文件不存在，可手动新建 `.codex` 目录，以及 `config.toml` / `auth.json` 两个文件。Windows 用户记得显示文件扩展名，避免变成 `config.toml.txt`。

![Mac 访达中找到 .codex 文件夹](image-7.png)

Mac 在用户目录中找到 `.codex`。

![.codex 文件夹中的 config.toml 和 auth.json](image-8.png)

确认存在 `config.toml` 和 `auth.json`。

### 3.1.2 修改 `config.toml` 与 `auth.json`

打开第二章“使用密钥”弹窗里的 Codex CLI 接入配置，把对应内容填入两个文件即可。三件事必须做对：

- `config.toml` 中的 `base_url` 必须是`https://api.sakms.top`。
- `auth.json` 中的 `api_key` 必须是你自己在第二章创建的 API Key。
- 文件名后缀必须正确，Windows 下不要变成 `config.toml.txt` 或 `auth.json.txt`。

![alt text](image-21.png)

图 10：配置示例，截图中的 API Key 已脱敏。请使用你自己的密钥，不要复制教程截图。

### 3.1.3 验证 Codex

- **CLI：** 在任意终端输入 `codex`，看是否能正常启动并响应。
- **VSCode 插件 / APP：** 重启编辑器或 APP，发起一次对话测试。

## 3.2 Claude Code 系列（CLI / VSCode 插件）

适用范围：**Claude Code CLI、VSCode Claude 插件**。参考页提供两种方式：环境变量或 `settings.json`，二选一即可，建议优先使用 `settings.json`，便于后续排查。

### 3.2.1 定位 Claude 配置目录

| 系统 | 配置目录 | 打开方式 |
| --- | --- | --- |
| Windows | `%userprofile%\.claude` | 按 `Win` + `R`，输入 `%userprofile%\.claude` 并回车。 |
| macOS | `~/.claude` | 终端执行 `open ~/.claude`。 |
| Linux | `~/.claude` | 终端执行 `cd ~/.claude` 或直接进入。 |

### 3.2.2 配置环境变量

复制第二章“使用密钥”弹窗中 Claude Code 给出的环境变量，通常包含 `ANTHROPIC_BASE_URL`、`ANTHROPIC_AUTH_TOKEN` 等。

| 系统 | 设置方法 |
| --- | --- |
| Windows | 控制面板 → 系统 → 高级系统设置 → 环境变量 → 新建用户变量；或用 PowerShell：`setx ANTHROPIC_BASE_URL "https://api.sakms.top/v1"`。 |
| macOS | 在 `~/.zshrc` 末尾追加：`export ANTHROPIC_BASE_URL="https://api.sakms.top/v1"`，保存后执行 `source ~/.zshrc`。 |
| Linux | 在 `~/.bashrc` 或 `~/.zshrc` 末尾追加同上 export 语句，再执行 `source`。 |

### 3.2.3 修改 `settings.json`（建议）
![alt text](image-22.png)
进入上表对应的 `.claude` 目录，编辑或新建 `settings.json`，按“使用密钥”弹窗给出的内容填写。

完成顺序：先设环境变量或改 `settings.json` → 重启终端 → 输入 `claude` 启动。

### 3.2.4 验证 Claude Code

打开新终端窗口（旧窗口不会读取新环境变量），输入 `claude`，能正常进入交互即成功。

## 3.3 Open Code

适用范围：**Open Code CLI**。Open Code 可写入 `opencode.json` 永久生效，也可以在客户端内用 `/connect` 临时切换。

### 3.3.1 定位 Open Code 配置目录

| 系统 | 配置目录 | 配置文件 |
| --- | --- | --- |
| Windows | `%USERPROFILE%\.config\opencode\` | `opencode.json` |
| macOS | `~/.config/opencode/` | `opencode.json` |
| Linux | `~/.config/opencode/` | `opencode.json` |

> 提示：配置文件不存在时需手动创建，也可使用支持注释的 `opencode.jsonc`。Windows 用户记得显示文件扩展名，避免变成 `opencode.json.txt`。

### 3.3.2 方式 A：写入 `opencode.json`（推荐，永久生效）

```json
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "sakms": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "SAKMS",
      "options": {
        "baseURL": "https://api.sakms.top/v1",
        "apiKey": "填写你的 API 密钥"
      },
      "models": {
        "gpt-5.5": { "name": "GPT-5.5" },
        "gpt-5.4": { "name": "GPT-5.4" }
      }
    }
  }
}
```

- `provider` 下的键名（这里写 `sakms`）可自定义，也可按客户端习惯使用 `openai` / `anthropic` / `google`。
- `models` 下可按需添加多个模型条目，具体模型 ID 以中转站提供的清单为准。
- 示例仅供参考，字段细节以 [Open Code 官方文档](https://opencode.ai/docs/config) 最新版为准。

### 3.3.3 方式 B：客户端内 `/connect` 命令（适合临时切换）

```text
/connect
```

启动 Open Code 后，在交互界面输入 `/connect`，按提示填入 `baseURL`（例如 `https://api.sakms.top/v1`）与 `apiKey`（你的 API Key）即可。此方式无需手动编辑 JSON，适合多账号或临时测试。

### 3.3.4 验证 Open Code

任意终端输入 `opencode` 启动客户端，发起一次对话测试，能正常返回响应即成功。

## 3.4 Open Claw

Open Claw 支持两种部署方式：**腾讯云在线配置**（推荐新手）和 **本地配置**。

### 3.4.1 腾讯云在线配置

**适用场景：** 你已经在腾讯云开通了“龙虾服务器”，希望直接通过云端面板接入。

1. 登录腾讯云，进入你的龙虾服务器控制台。
2. 点击“应用管理” → 找到“模型”模块。
3. 在下拉菜单中选择“自定义模型” → “JSON 输入”。
4. 把下方 JSON 粘贴进去，替换 API Key 后点击“添加并应用”。

```json
{
  "provider": "openai",
  "base_url": "https://api.sakms.top/v1",
  "api": "openai-completions",
  "api_key": "填写你的 API 密钥",
  "model": {
    "id": "gpt-5.5",
    "name": "GPT-5.5"
  }
}
```

> 注意：`api_key` 字段务必替换为自己的 API Key，不要直接提交带有占位符的 JSON。

### 3.4.2 本地配置（Windows / macOS / Linux）

| 系统 | 配置目录 | 打开方式 |
| --- | --- | --- |
| Windows | `%USERPROFILE%\.openclaw` | 按 `Win` + `R`，输入 `%USERPROFILE%\.openclaw` 并回车。 |
| macOS | `~/.openclaw` | 终端执行 `open ~/.openclaw`。 |
| Linux | `~/.openclaw` | 终端执行 `xdg-open ~/.openclaw` 或直接进入。 |

进入上述目录，编辑或新建 `openclaw.json`，写入：

```json
{
  "baseUrl": "https://api.sakms.top/v1",
  "api": "openai-responses",
  "apiKey": "填写你的 API 密钥"
}
```

> 提示：Open Claw 本地配置通常使用 `openai-responses`，与腾讯云端示例里的 `openai-completions` 不同，不要混用。

## 3.5 配置方式二：使用 cc-switch 一键配置

新手可以使用 cc-switch 一键配置秘钥。下载后选择右上角“+”，选择 Codex 供应商，再选择自定义配置。

cc-switch releases：<https://github.com/farion1231/cc-switch/releases>

1. API KEY 填写上一步创建得到的 API Key。
2. API 地址填写 `https://api.sakms.top/`。
3. 回到主页面点击开启，重新打开 Codex 即可正常使用。

![cc-switch 添加 Codex 供应商并选择自定义配置](image-15.png)

图 11：cc-switch 添加 Codex 供应商，并选择自定义配置。

## 3.6 重新打开 Codex 并使用 API 登录

完成文件配置后重新打开 Codex，选择“换种方式登录”，再选择 API 登录并粘贴自己的 API 密钥。

![Codex 选择换种方式登录](image-10.png)

图 12：选择“换种方式登录”。

![Codex API 登录输入框](image-11.png)

图 13：选择 API 登录并粘贴密钥。

![中转服务 API 密钥复制位置，密钥已脱敏](image-12.png)

图 14：从中转后台复制你自己的 API Key。

---

# 第四章 验证与排错

## 4.1 一行命令自检（推荐）

复制下方命令到终端，把 `sk-xxxx` 换成你的真实密钥。如果能返回模型清单，说明 Key 与 base_url 基本正常。

```bash
curl https://api.sakms.top/v1/models \
  -H "Authorization: Bearer sk-xxxx"
```

- **Windows PowerShell** 用户请把 `\` 续行符换成反引号 `` ` ``，或直接写成单行。
- 如果你的“使用密钥”弹窗显示的 base_url 不是 `/v1` 结尾，请以弹窗为准调整命令。

## 4.2 登录失败时快速检查

- 编辑配置前是否已经完全关闭 Codex？
- `config.toml` 和 `auth.json` 是否放在同一个 `.codex` 目录？
- 是否粘贴了你自己生成的 API Key，而不是教程截图中的示例？
- 创建密钥时是否按来源选择正确分组：质保网站补发的中转码选“质保补偿”，链动小铺额度兑换码选“GPT-Plus”？
- 如果使用 cc-switch，API 地址是否填写为 `https://api.sakms.top/`，并在主页面点击开启后重新打开 Codex？

## 4.3 常见报错对照表

| 报错 / 现象 | 原因 | 处理方式 |
| --- | --- | --- |
| `401 Unauthorized` / Incorrect API key | 密钥错、被删，或配置时 Codex 已经在运行。 | 关闭 Codex，回第二章重新创建或复制密钥，再重新配置 `config.toml` 并登录。 |
| `404 Not Found` | `base_url` 写错，或客户端需要 `/v1` 但未填写。 | 检查是否与“使用密钥”弹窗一致；OpenAI-compatible 客户端通常使用 `https://api.sakms.top/v1`。 |
| `余额不足` / `quota exceeded` / `429 Too Many Requests` | 充值未到账、额度用完、订阅日额度耗尽或触发频率限制。 | 打开额度查询页面 <https://api.sakms.top/profile> 查看余额和额度；必要时等待刷新或补充额度。 |
| `model not found` | 模型 ID 拼错，或当前分组不支持该模型。 | 用 4.1 的 curl 命令查看模型清单，或按中转后台可用模型重新填写。 |
| 客户端启动后无反应 | 环境变量未生效，旧终端还在使用旧配置。 | 关闭并新开一个终端窗口，再启动 `codex` / `claude` / `opencode`。 |
| `config.toml.txt` / `opencode.json.txt` | Windows 默认隐藏文件后缀，实际创建成了文本文件。 | 资源管理器 → 查看 → 勾选“文件扩展名”，再把文件名改正确。 |
| 原 Codex 聊天记录不见 | 切换 API 中转后，本地 provider 不一致。 | 参考 <https://github.com/Dailin521/codex-provider-sync/releases> 恢复。 |

![Codex 登录时报 401 Incorrect API key，密钥已脱敏](image-14.png)

图 15：错误示例，API Key 片段已脱敏。

---

# 第五章 FAQ

**Q1：一个账号能创建几把密钥？**

可创建多把，建议按客户端 / 设备分别命名，便于审计和单独吊销。

**Q2：泄露密钥怎么办？**

立即在“API 密钥”页面删除原密钥，重新创建一把，然后更新所有客户端的配置。

**Q3：Codex / Claude Code / Open Code 可以共用一把密钥吗？**

可以，但不建议。多把密钥独立计费、独立吊销，出问题时排查更快。

**Q4：充值的钱和订阅额度怎么消费？**

额度模式按调用量从余额扣款；订阅模式则在订阅周期或每日额度内使用，具体计费规则以中转后台实时显示为准。

**Q5：技术支持怎么联系？**

可查看页面上方交流群二维码，或登录中转站后在个人中心、站内公告、客服入口查看最新联系方式。
